/**
 * @fileoverview Primary Definitions
 */

/**
 * Primary Extruders (Extruder definitions bundled with "cura-wasm" itself)
 */
export const primaryExtruders = ['fdmextruder'];

/**
 * Primary Printers (Printer definitions bundled with "cura-wasm" itself)
 */
export const primaryPrinters = ['fdmprinter'];
